apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-soar
spec:
  selector:
    app: {{ .Release.Name }}-soar
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
